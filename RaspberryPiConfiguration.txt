lsusb - (list usb)
sudo apt install fswebcam - (install webcam library)

## Create Virtual Environment

python3 -m venv .venv
source .venv/bin/activate
python3 -m pip install -r requirements.txt

## Pip Install